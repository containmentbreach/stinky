{
  "name": "stinky",
  "version": "0.1.1",
  "description": "",
  "main": "lib/index.js",
  "scripts": {
    "prepublish": "npm run build",
    "build": "lsc -m embedded -o lib -c src/*.ls",
    "clean": "rm *.js",
    "test": "mocha --ui qunit --compilers ls:livescript -t 10s"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "string-hash": "^1.1.0"
  },
  "devDependencies": {
    "express": "^4.13.4",
    "livescript": "^1.4.0",
    "mocha": "^2.4.5",
    "request": "^2.69.0",
    "requests": "^0.1.7",
    "supertest": "^1.2.0"
  }
}
